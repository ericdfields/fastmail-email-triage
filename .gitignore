node_modules/
dist/
.env
output/
.worktrees/

# Local Claude Code state — settings.local.json accumulates approved commands,
# which can include connection strings. Never commit it (this repo is public).
.claude/settings.local.json

.DS_Store
*.log

# Private local inputs (job profile, LinkedIn connection exports). This repo is public.
private/

# Generated at runtime (morning digest snapshots).
data/
